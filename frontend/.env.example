# Base URL for the backend API. In local dev this is unset and requests go
# through the Vite proxy to http://127.0.0.1:8000. In production set this to
# your deployed backend, e.g. https://api.yourdomain.com
VITE_API_BASE_URL=
